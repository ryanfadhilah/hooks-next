import HookRef from "@/components/content/Ref";
import React from "react";

export const metadata = {
  title: "Hooks Notes - Ref",
  description: "Personal Hooks Notes for ref",
};

const page = () => {
  return (
    <div className="flex w-full h-full">
      <HookRef></HookRef>
    </div>
  );
};

export default page;
