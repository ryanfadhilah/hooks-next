import HookImperativeHandler from "@/components/content/Imperative";
import React from "react";

export const metadata = {
  title: "Hooks Notes - ImperativeHandler",
  description: "Personal Hooks Notes for ImperativeHandler",
};

const page = () => {
  return (
    <div className="flex w-full h-full">
      <HookImperativeHandler></HookImperativeHandler>
    </div>
  );
};

export default page;
