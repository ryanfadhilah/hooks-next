import HookEffect from "@/components/content/Effect";
import React from "react";

export const metadata = {
  title: "Hooks Notes - Effect",
  description: "Personal Hooks Notes for Effect",
};

const page = () => {
  return (
    <div className="flex w-full h-full">
      <HookEffect></HookEffect>
    </div>
  );
};

export default page;
